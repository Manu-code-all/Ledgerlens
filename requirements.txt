# Data generation and the rule-based matcher use only the Python standard
# library, so nothing here is needed yet. These arrive in later stages.

anthropic>=0.40.0      # the reconciliation agent
fastapi>=0.115.0       # API layer for the React dashboard
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9  # file uploads
python-dotenv>=1.0.0   # loads ANTHROPIC_API_KEY from .env
