# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Secrets — never commit the API key
.env
.env.*
!.env.example

# Node / React
node_modules/
dist/
.vite/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Run outputs (regenerate with: python engine/generate_data.py)
data/audit_log.jsonl
data/audit_log_baseline.jsonl
data/audit_log_full.jsonl
data/results.json
data/uploaded_settlements.csv
data/uploaded_bank.csv

# Pitch video assets — raw footage, audio, screen recordings (large binaries,
# not part of the deployable project; the finished video is submitted separately)
PITCH/
